<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome | RS Mani Cafe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #ffffff 50%, #f8b400 50%);
            height: 100vh;
            color: #4b2e20;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            font-family: 'Poppins', sans-serif;
        }
        .login-container {
            background: rgba(255, 255, 255, 0.9);
            padding: 3rem;
            border-radius: 10px;
            text-align: center;
            width: 90%;
            max-width: 450px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            color: #4b2e20;
        }
        .logo {
            width: 100px; /* Adjust the logo size as needed */
            height: auto;
            margin-bottom: 1rem;
        }
        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: #4b2e20;
        }
        p {
            height: 2rem;
            margin-bottom: 2rem;
        }
        .btn-primary, .btn-outline-light {
            font-size: 1.2rem;
            font-weight: 700;
            padding: 0.75rem 2rem;
            margin: 0.5rem;
            transition: background-color 0.3s ease, color 0.3s ease;
            border-radius: 5px;
        }
        .btn-primary {
            background-color: #ff9000;
            border: none;
            color: #4b2e20;
        }
        .btn-primary:hover {
            background-color: #cc7400;
            color: #fff;
        }
        .btn-outline-light {
            border: 2px solid #ff9000;
            color: #ff9000;
        }
        .btn-outline-light:hover {
            background-color: #ff9000;
            color: #fff;
        }
        footer {
            position: absolute;
            bottom: 1rem;
            left: 50%;
            transform: translateX(-50%);
            color: #ffffffb3;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <!-- Logo Image Positioned Above the Header -->
        <img src="/images/logo.jfif" alt="Cafe Logo" class="logo">
        <h1>Welcome to RS Mani Cafe</h1>
        <p></p> <!-- Empty paragraph tag to create space -->
        <div class="login-buttons">
            <a href="/auth/login" class="btn btn-primary">Login</a>
            <a href="/auth/register" class="btn btn-outline-light">Register</a>
        </div>
    </div>

    <footer>&copy; 2024 RS Mani Cafe | Savor the South Indian Experience!</footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
