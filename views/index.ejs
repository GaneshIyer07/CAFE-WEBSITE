<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome | RS Mani Cafe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #3b5998, #8b9dc3); /* Professional gradient from blue to light blue */
            height: 100vh;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            font-family: Arial, sans-serif;
        }
        .login-container {
            background-color: rgba(0, 0, 0, 0.65);
            padding: 3rem;
            border-radius: 10px;
            text-align: center;
            width: 90%;
            max-width: 450px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Soft shadow for depth */
        }
        h1 {
            font-family: 'Georgia', serif;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: #fff9c4; /* Softer yellow for title */
        }
        p {
            font-size: 1.1rem;
            color: #ffffffb3; /* Lighter white for subtitle text */
            margin-bottom: 2rem;
        }
        .btn-primary, .btn-outline-light {
            font-size: 1.25rem;
            padding: 0.75rem 2rem;
            margin: 0.5rem;
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        .btn-primary {
            background-color: #f8b400;
            border: none;
            color: #fff;
        }
        .btn-primary:hover {
            background-color: #d39400;
        }
        .btn-outline-light {
            border: 2px solid #f8b400;
            color: #f8b400;
        }
        .btn-outline-light:hover {
            background-color: #f8b400;
            color: #fff;
        }
        footer {
            position: absolute;
            bottom: 1rem;
            left: 50%;
            transform: translateX(-50%);
            color: #ffffffb3;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>Welcome to RS Mani Cafe</h1>
        <p>Indulge in authentic South Indian flavors crafted with passion and served with care.</p>
        <div class="login-buttons">
            <a href="/auth/login" class="btn btn-primary">Login</a>
            <a href="/auth/register" class="btn btn-outline-light">Register</a>
        </div>
    </div>

    <footer>&copy; 2024 RS Mani Cafe | Savor the South Indian Experience!</footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
