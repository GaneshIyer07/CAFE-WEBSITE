<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome | RS Mani Cafe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #ffffff 50%, #f8b400 50%); /* Upper half white, lower half yellow */
            height: 100vh;
            color: #4b2e20; /* Dark brown text color */
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
            font-family: 'Poppins', sans-serif; /* Modern font */
        }
        .login-container {
            background: linear-gradient(to bottom, #f8b400 50%, #ffffff 50%); /* Upper half yellow, lower half white */
            padding: 3rem;
            border-radius: 10px;
            text-align: center;
            width: 90%;
            max-width: 450px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            color: #4b2e20; /* Dark brown text for contrast */
        }
        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: #4b2e20; /* Dark brown for title */
        }
        p {
            height: 2rem; /* Adds fixed space below the welcome message */
            margin-bottom: 2rem; /* Additional space if needed */
        }
        .btn-primary, .btn-outline-light {
            font-size: 1.2rem;
            font-weight: 700;
            padding: 0.75rem 2rem;
            margin: 0.5rem;
            transition: background-color 0.3s ease, color 0.3s ease;
            border-radius: 5px;
        }
        .btn-primary {
            background-color: #ff9000; /* Bold, vibrant orange */
            border: none;
            color: #4b2e20; /* Dark text color for contrast */
        }
        .btn-primary:hover {
            background-color: #cc7400; /* Darker orange on hover */
            color: #fff;
        }
        .btn-outline-light {
            border: 2px solid #ff9000;
            color: #ff9000;
        }
        .btn-outline-light:hover {
            background-color: #ff9000;
            color: #fff;
        }
        footer {
            position: absolute;
            bottom: 1rem;
            left: 50%;
            transform: translateX(-50%);
            color: #ffffffb3;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>Welcome to RS Mani Cafe</h1>
        <p></p> <!-- Empty paragraph tag to create space -->
        <div class="login-buttons">
            <a href="/auth/login" class="btn btn-primary">Login</a>
            <a href="/auth/register" class="btn btn-outline-light">Register</a>
        </div>
    </div>

    <footer>&copy; 2024 RS Mani Cafe | Savor the South Indian Experience!</footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
