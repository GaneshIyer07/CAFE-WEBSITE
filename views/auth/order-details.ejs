<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Receipt - RS MANI Cafe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            padding: 20px;
        }

        .order-container {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
        }

        .order-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .order-header h2 {
            color: #333;
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .order-details table {
            width: 100%;
            margin-top: 20px;
        }

        .order-details th, .order-details td {
            padding: 12px;
            text-align: center;
        }

        .order-status {
            margin-top: 30px;
            text-align: center;
            font-size: 1.2rem;
            color: #333;
        }

        .status-select {
            padding: 8px;
            font-size: 1.1rem;
        }

        .order-total {
            text-align: center;
            margin-top: 20px;
            font-size: 1.5rem;
            font-weight: bold;
        }

        .note {
            font-size: 1rem;
            color: #666;
            margin-top: 10px;
        }

        .back-btn {
            text-align: center;
            margin-top: 30px;
        }

        .btn-back {
            background-color: #0069d9;
            color: white;
            font-size: 1rem;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
        }

        .btn-back:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="order-container">
        <div class="order-header">
            <h2>Order Receipt</h2>
            <p><strong>Order No:</strong> <%= order._id %></p>
            <p><strong>Username:</strong> <%= order.userId.username %></p>
            <p><strong>Order Date:</strong> <%= order.createdAt.toLocaleString() %></p>
        </div>

        <div class="order-details">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Item Name</th>
                        <th>Quantity</th>
                        <th>Price (₹)</th>
                        <th>Total (₹)</th>
                    </tr>
                </thead>
                <tbody>
                    <% let totalAmount = 0; %>
                    <% order.items.forEach(item => { %>
                        <tr>
                            <td><%= item.itemId.name %></td>
                            <td><%= item.quantity %></td>
                            <td>₹<%= item.itemId.price %></td>
                            <td>₹<%= item.itemId.price * item.quantity %></td>
                        </tr>
                        <% totalAmount += item.itemId.price * item.quantity; %>
                    <% }) %>
                </tbody>
            </table>

            <div class="order-total">
                <p><strong>Total Amount: ₹<%= totalAmount %></strong></p>
            </div>
        </div>

        <div class="order-status">
            <h4>Order Status: <%= order.orderStatus %></h4>
            <% if (order.orderStatus !== 'Delivered') { %>
                <p class="note">Note: The status is updated by the admin. Please check again later.</p>
            <% } %>
        </div>

        <div class="back-btn">
            <a href="/dashboard/orders" class="btn-back">Back to Orders</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
