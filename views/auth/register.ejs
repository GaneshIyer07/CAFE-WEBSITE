<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="/css/output.css" rel="stylesheet">
    <style>
        /* Ensure the body takes full height for flex centering */
        body, html {
            height: 100%;
            margin: 0;
        }

        /* New background with half yellow and half white */
        body {
            background: linear-gradient(to bottom, #f8b400 50%, #ffffff 30%); /* Yellow on top, white on bottom */
            color: #4b2e20; /* Dark brown text */
        }

        /* Styling for Navbar */
        .navbar {
            background-color: #ffffff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            position: fixed;
            width: 100%;
            z-index: 10;
        }
        .navbar-content {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1rem;
            gap: 2rem;
        }
        .navbar-link {
            font-size: 1.125rem; /* Equivalent to text-lg */
            color: #4b2e20;
            font-weight: 600;
            transition: color 0.3s;
        }
        .navbar-link:hover {
            color: #1d4ed8; /* Blue on hover */
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav class="navbar">
        <div class="navbar-content">
            <!-- RS MANI Cafe Logo -->
            <a href="/" class="text-2xl font-bold text-yellow-500">RS MANI Cafe</a>
            <!-- Home Link -->
            <a href="/auth/dashboard" class="navbar-link">Home</a>
        </div>
    </nav>

    <!-- Centering the form in the middle -->
    <div class="flex items-center justify-center" style="height: 100vh; padding-top: 6rem;">
        <div class="max-w-md w-full bg-white p-6 shadow-lg rounded">
            <h2 class="text-2xl font-bold mb-4 text-center">Register</h2>
            
            <form action="/auth/register" method="POST">
                <div class="mb-4">
                    <label for="username" class="block text-gray-700">Username</label>
                    <input type="text" name="username" class="w-full p-2 border border-gray-300 rounded" required>
                </div>
                <div class="mb-4">
                    <label for="email" class="block text-gray-700">Email</label>
                    <input type="email" name="email" class="w-full p-2 border border-gray-300 rounded" required>
                </div>
                <div class="mb-4">
                    <label for="password" class="block text-gray-700">Password</label>
                    <input type="password" name="password" class="w-full p-2 border border-gray-300 rounded" required>
                </div>
                <!-- Register button with customized font -->
                <button type="submit" class="bg-blue-500 text-blue-950 py-2 px-4 rounded hover:bg-blue-600 font-bold w-full">Register</button>
            </form>
        </div>
    </div>

    <script src="/node_modules/flowbite/dist/flowbite.js"></script>
</body>
</html>
