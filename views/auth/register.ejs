<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link href="/css/output.css" rel="stylesheet">
    <style>
        /* Ensure the body takes full height for flex centering */
        body, html {
            height: 100%;
            margin: 0;
        }

        /* New background with half yellow and half white */
        body {
            background: linear-gradient(to bottom, #f8b400 50%, #ffffff 30%); /* Yellow on top, white on bottom */
            color: #4b2e20; /* Dark brown text */
        }
    </style>
</head>
<body>

    <!-- Navigation Bar -->
    <nav class="bg-white shadow w-full">
        <div class="container mx-auto px-4 py-4">
            <div class="flex justify-between">
                <a href="/" class="text-2xl font-bold text-yellow-500">RS MANI Cafe</a>
                <div>
                    <!-- Additional navigation items can go here -->
                </div>
            </div>
        </div>
    </nav>

    <!-- Centering the form in the middle -->
    <div class="flex items-center justify-center" style="height: 100vh;">
        <div class="max-w-md w-full bg-white p-6 shadow-lg rounded">
            <h2 class="text-2xl font-bold mb-4 text-center">Register</h2>
            
            <!-- Success Message -->
            <% if (successMessage) { %>
                <div class="mb-4 text-green-800 p-2 rounded">
                    <%= successMessage %>
                </div>
                <script>
                    // Redirect to login after 3 seconds
                    setTimeout(function() {
                        window.location.href = "/auth/login"; // Redirect to login page
                    }, 3000);
                </script>
            <% } %>
            
            <form action="/auth/register" method="POST">
                <div class="mb-4">
                    <label for="username" class="block text-gray-700">Username</label>
                    <input type="text" name="username" class="w-full p-2 border border-gray-300 rounded" required>
                </div>
                <div class="mb-4">
                    <label for="email" class="block text-gray-700">Email</label>
                    <input type="email" name="email" class="w-full p-2 border border-gray-300 rounded" required>
                </div>
                <div class="mb-4">
                    <label for="password" class="block text-gray-700">Password</label>
                    <input type="password" name="password" class="w-full p-2 border border-gray-300 rounded" required>
                </div>
                <!-- Register button with customized font -->
                <button type="submit" class="bg-blue-500 text-black py-2 px-4 rounded hover:bg-blue-600 font-bold w-full">Register</button>
            </form>
        </div>
    </div>

    <script src="/node_modules/flowbite/dist/flowbite.js"></script>
</body>
</html>
