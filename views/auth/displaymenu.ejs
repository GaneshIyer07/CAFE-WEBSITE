<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu | RS Mani Cafe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #0069d9, #ffdd57); /* Blue gradient background */
            color: white;
            margin: 0;
            display: flex;
        }
        /* Sidebar Styles */
        .sidebar {
            background-color: rgba(0, 0, 0, 0.8); /* Semi-transparent black */
            width: 250px; /* Fixed width for sidebar */
            height: 100vh; /* Full height */
            position: fixed; /* Fix to the side */
            top: 0;
            left: 0;
            padding-top: 20px; /* Spacing at the top */
        }
        .sidebar a {
            padding: 10px 15px; /* Padding for links */
            text-decoration: none;
            color: #ffdd57; /* Soft yellow for sidebar links */
            display: block; /* Block display for easy clicking */
        }
        .sidebar a:hover {
            background-color: rgba(255, 255, 255, 0.2); /* Hover effect */
        }
        .content {
            margin-left: 250px; /* Leave space for the sidebar */
            padding: 20px; /* Padding around the content */
            width: calc(100% - 250px); /* Full width minus sidebar width */
        }
        .menu-header {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 20px;
            color: #fff9c4; /* Soft yellow for headers */
        }
        .card {
            transition: transform 0.3s ease;
            border: none; /* Remove card borders */
            background-color: rgba(255, 255, 255, 0.1); /* Semi-transparent white */
            color: #ffdd57; /* Soft yellow for card text */
        }
        .card:hover {
            transform: translateY(-10px); /* Hover effect */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Add shadow */
        }
        .card-title {
            font-size: 1.5rem;
            color: #ffdd57; /* Soft yellow for card titles */
        }
        .card-subtitle {
            font-size: 1.2rem;
            color: #fff9c4; /* Light yellow for subtitles */
        }
        .btn-danger {
            background-color: #dc3545; /* Red color for the logout button */
            border: none;
        }
        .btn-danger:hover {
            background-color: #c82333; /* Darker red on hover */
        }
        footer {
            background-color: #1f2937;
            color: #ffdd57;
            padding: 20px 0;
            text-align: center;
            position: fixed; /* Fix the footer to the bottom */
            width: calc(100% - 250px); /* Full width minus sidebar */
            left: 250px; /* Align with content area */
            bottom: 0; /* Align at the bottom */
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h2 class="text-center text-warning">RS MANI CAFE</h2>
        <a href="/auth/dashboard">Home</a>
        <a href="/auth/logout">Logout</a>
        <!-- You can add more sidebar links here -->
    </div>

    <!-- Content Area -->
    <div class="content">
        <div class="container">
            <!-- Menu Header -->
            <h1 class="text-center menu-header">Welcome, <%= user.username %>! Here is our Menu:</h1>

            <!-- Menu Items -->
            <div class="row mb-4">
                <% menuItems.forEach(item => { %>
                    <div class="col-md-4 mb-3">
                        <div class="card h-100 text-center p-4">
                            <div class="card-body">
                                <h5 class="card-title"><strong><%= item.name %></strong></h5>
                                <h6 class="card-subtitle mb-2 text-muted">₹<%= item.price %></h6>
                                <p class="card-text"><em><%= item.description %></em></p>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer>&copy; 2024 RS Mani Cafe | Authentic South Indian Flavors!</footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
