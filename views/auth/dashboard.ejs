<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | RS Mani Cafe</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #fff3e0; /* Warm background tone */
        }
        nav {
            background-color: #4a2c2a; /* Dark brown */
            padding: 15px;
        }
        nav .brand {
            color: #ffdd57; /* Warm yellow */
            font-size: 28px;
            font-weight: bold;
        }
        nav .nav-links a {
            color: #ffdd57;
            margin-left: 20px;
            text-decoration: none;
            font-size: 16px;
            font-weight: bold;
        }
        nav .nav-links a:hover {
            color: #fff9c4; /* Lighter yellow on hover */
        }
        .container {
            margin-top: 40px;
            text-align: center;
        }
        .title {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 40px;
            color: #4a2c2a; /* Dark brown for text */
        }
        h2 {
            color: #ff6f61; /* South Indian earthy orange */
        }
        .grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr); /* 3 columns */
            gap: 20px;
            justify-items: center;
        }
        .icon-card {
            width: 100%;
            max-width: 200px; /* Adjust icon card size */
            background-color: #fff; /* White card */
            border-radius: 12px;
            border: 1px solid #ff6f61; /* Subtle border */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out;
            cursor: pointer;
            padding: 30px;
            text-align: center;
            color: #4a2c2a;
        }
        .icon-card:hover {
            transform: translateY(-10px);
        }
        .icon-card .icon {
            font-size: 48px; /* Adjust icon size */
            margin-bottom: 15px;
            color: #ff6f61; /* Icon color */
        }
        .icon-card h3 {
            font-size: 18px;
            margin: 10px 0;
            color: #4a2c2a;
        }
        body {
        display: flex;
        flex-direction: column;
         min-height: 100vh; /* Ensure full viewport height */
        }

        footer {
        text-align: center;
        margin-top: auto; /* Push footer to the bottom */
        padding: 20px 0;
        background-color: #1f2937; /* Dark background for footer */
        color: #fff; /* White text */
        }

        @media(max-width: 1024px) {
            .grid {
                grid-template-columns: repeat(2, 1fr); /* 2 columns on smaller screens */
            }
        }
        @media(max-width: 768px) {
            .grid {
                grid-template-columns: 1fr; /* 1 column on mobile */
            }
        }
    </style>
    <!-- Adding FontAwesome for icons -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</head>
<body>

    <nav>
        <div class="container mx-auto flex justify-between items-center">
            <div class="brand">RS MANI CAFE</div>
            <div class="nav-links">
                <a href="/">Home</a>
                <a href="/auth/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-8">
        <h1 class="title">Hello <%= user.username %>,</h1>
        <h2 class="text-2xl mb-6">Welcome to RS MANI CAFE</h2>

        <!-- Icon-Based Card Grid -->
        <div class="grid">
            <!-- View Menu Icon Card -->
            <div class="icon-card" onclick="location.href='/auth/dashboard/menu'">
                <div class="icon">
                    <i class="fas fa-utensils"></i> <!-- Food-related icon -->
                </div>
                <h3>View Menu</h3>
            </div>

            <!-- Outlets Icon Card -->
            <div class="icon-card" onclick="location.href='/auth/dashboard/outlets'">
                <div class="icon">
                    <i class="fas fa-map-marker-alt"></i> <!-- Map/Outlets icon -->
                </div>
                <h3>Outlets</h3>
            </div>

            <!-- Order Icon Card -->
            <div class="icon-card" onclick="location.href='/order'">
                <div class="icon">
                    <i class="fas fa-shopping-cart"></i> <!-- Order icon -->
                </div>
                <h3>Place Order</h3>
            </div>
        </div>
    </div>

    <footer>&copy; 2024 RS Mani Cafe | Serving Authentic South Indian Flavors!</footer>

    <script src="/node_modules/flowbite/dist/flowbite.js"></script>
</body>
</html>
